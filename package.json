{
  "name": "fifa-world-cup-2026-sweepstake",
  "private": true,
  "scripts": {
    "dev": "npx --yes netlify-cli dev",
    "start": "npx --yes netlify-cli dev",
    "verify": "node scripts/verify-scores-and-boards.mjs",
    "verify:fixes": "node scripts/verify-fixes.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.9"
  }
}
